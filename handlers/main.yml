---
# handlers file for phpmyadmin
- name: restart httpd
  service:
    name: "{{ item }}"
    state: restarted
  when:
    - ansible_virtualization_type != "docker"
  with_items:
    - "{{ zabbix_httpd_services[ansible_distribution ~ '-' ~ ansible_distribution_major_version]
      | default(zabbix_httpd_services[ansible_distribution]
      | default(zabbix_httpd_services['default'] )) }}"
